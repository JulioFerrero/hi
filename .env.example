# PostgreSQL connection (used by all packages and apps)
DATABASE_URL=postgresql://webbuilder:webbuilder@localhost:5432/webbuilder

# Set this to the website ID after running `pnpm db:seed`
# The seed script will print the ID for you
WEBSITE_ID=
